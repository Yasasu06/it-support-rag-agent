[build]
builder = "nixpacks"

[deploy]
startCommand = "python3 -m spacy download en_core_web_sm && python3 ingest_all.py && streamlit run app.py --server.port=8501 --server.address=0.0.0.0"
restartPolicyType = "on_failure"
